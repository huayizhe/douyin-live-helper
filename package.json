{
  "name": "douyin-live-helper",
  "version": "1.4.11",
  "description": "抖音关注直播助手",
  "main": "content.js",
  "scripts": {
    "watch": "rollup -c -w",
    "build": "rollup -c",
    "sync": "node scripts/release.js",
    "pack": "node scripts/pack-zip.js",
    "release": "rollup -c && node scripts/release.js && node scripts/pack-zip.js",
    "test": "node --test test/**/*.test.mjs"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^19.0.2",
    "@rollup/plugin-node-resolve": "^11.2.1",
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^2.60.0"
  },
  "license": "CC-BY-NC-4.0",
  "private": true,
  "dependencies": {
    "douyin-live-helper": "file:"
  }
}
