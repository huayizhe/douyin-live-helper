# 依赖（可由 npm install 重新生成）
node_modules/

# 敏感信息 —— 许可证私钥、环境变量，绝不可进版本库
.env
*.env
server/.env
tools/private-key.json

# ---------- 仅本地保留、不上传远程仓库 ----------
# 开发文档（本地维护，不进入公开仓库）
DEV.md

# 付费 / 运营相关（本地可留，不上传）
docs/
tools/
server/

# 介绍截图目录（已废弃）
介绍图片/

# 运行时数据
server/*.db
server/*.db-shm
server/*.db-wal
server/orders.json
*.log

# 打包产物：忽略其它 zip，但允许提交版本化安装包
*.zip
!抖音关注直播助手-v*.zip

# 系统 / 编辑器
.DS_Store
Thumbs.db
.claude/
