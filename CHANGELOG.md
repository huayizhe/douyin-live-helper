# 更新日志

本文件记录「抖音关注直播助手」的主要功能迭代与修复。
版本号遵循 `主版本.次版本.修订` 约定；日期格式 `YYYY-MM-DD`。

---

## [1.4.9] - 2026-07-28

### 修复 / 体验
- **CDN 失败本地兜底**：赞赏/群码优先 jsDelivr；失败则用扩展包 `hosted/` 同名图。
- **赞赏标题**：改为「赞赏作者（自愿支持）」。

---

## [1.4.8] - 2026-07-28

### 体验
- **赞赏/交流弹窗加大**：宽约 720、二维码 260×260；标题说明改到图片下方并居中。
- **图标调整**：「赞赏/交流」改为交流气泡；「资源/设置」改为齿轮。
- **赞赏码改 CDN 托管**（与群码一致）：`hosted/donate-qr.png` + jsDelivr；去掉扩展包内嵌与 `web_accessible_resources`。
- **修复抖音页二维码空白**：页面 CSP 拦截外链 `<img>`；改为扩展 `fetch` CDN 再转 `blob:` 显示（README 直链仍正常）。
- **群码过期提示**：联系 `1035864725@qq.com`，备注「插件交流进群」。
- **README 安装首行**：提示下载 / 使用 `douyin_live_helper_plugin` 目录。

---

## [1.4.7] - 2026-07-28

### 功能
- **赞赏/交流入口**（`support.js` / `modal.js`）：顶栏「N 个直播」左侧新增「赞赏/交流」按钮；弹窗左右展示赞赏码与微信群码，统一 180×180。
- **赞赏码打进扩展包**（`icons/qr-donate.png` + `web_accessible_resources`），本地展示、长期有效。
- **微信群码 CDN 托管**（`hosted/wechat-group-qr.jpg` + jsDelivr）：插件只读托管链接；换群码覆盖文件并把 `GROUP_QR_CACHE_BUST` +1 后推送即可，用户无需重装。

### 测试 / 文档
- 新增 `test/support-qr.test.mjs`；同步 README / DEV / `hosted/README.md`。

---

## [1.4.6] - 2026-07-25

### 体验
- **放宽视口播放门控**（`modal.js` / `playback-gate.js`）：循环播放由「须完整可见」改为露出约 1/3（`PLAY_VISIBLE_RATIO=0.35`）即可播，并加上下 `PLAY_ROOT_MARGIN=300px`（约一排缓冲）；加载观察器 `_clipObserver` 不变。半截滚出仍可播约两排，离开扩展视口才 pause。
- **三 Tab 面板固定壳高**（`monitor.js`）：`height`/`maxHeight` 同为 `min(86vh, 720px)`；`mon-body` 用 `flex:1; min-height:0` 内部滚动。切到「数据备份」时窗口高度与监控/设置一致，不再因内容少塌缩。
- **悬浮预览与循环衔接**（`preview.js`）：停留 ≥200ms 后仍 `pauseLoading('hover')`，但**不立刻** `pauseCard`；hover live `z-index:2` 叠在循环之上，`play` 成功且 opacity=1 后再 pause 本卡；取流/播放失败则循环继续。

### 测试 / 文档
- 单测覆盖播放门控阈值、面板壳高、悬浮 pause 时机与 z-index。
- 版本升至 1.4.6。

---

## [1.4.5] - 2026-07-25

### 修复
- **横屏循环被毛玻璃挡住**（`utils.js` / `preload.js`）：`.dy-media-blur` 固定 `z-index:0`，循环/直播 video 固定 `z-index:1`；`_removeLoopVideo` / `release` 顺带清残留 blur 与 `_clipHls`，避免滚回重挂时同层后绘的 blur 盖住画面。

### 体验
- **稳播门控 + 录中丢弃重录**（`preload.js` / `clip-stutter.js`）：`playing` 后约 1s 确认 `currentTime` 持续推进再申请录制槽；录中连续停滞 >300ms 或停滞占比 >15% 则丢弃 blob、保持 live、稍后重录。

### 设置
- **去掉自动/手动**：删除 `perfMode`；滑块值即生效并发。一键还原时加载/录制并发按本机核数推荐，其余回产品默认；旁注显示推荐值。
- **清晰度四档**（`clipQuality` 0–3）：标清/高清/超清/蓝光，同时控制列表拉流（SD1→FULL_HD1）与录制码率（400k→2M）；旧 `clipBitrate` 自动迁移。每项补充作用说明；三 Tab 面板等高（`min-height`）。

### 测试 / 文档
- 单测覆盖无 `perfMode`、还原按核数、清晰度档映射、稳播/卡顿纯函数。
- 版本升至 1.4.5。

---

## [1.4.4] - 2026-07-25

### 功能
- **资源/设置三标签页**（`monitor.js` / `modal.js`）：顶栏「资源」改为「资源/设置」，统一浮层面板含三个 Tab——资源监控、插件设置、数据备份；打开默认落在监控；监控仅在该 Tab 激活时 1s 采样（切走停采样）。去掉顶栏独立「备份」按钮，导入/导出特别关心迁入第三 Tab。
- **直播墙性能参数可配置**（`settings.js` / `preload.js`）：插件设置 Tab 可调同时加载/录制路数、片段时长/码率、进视口停留、缓存上限、过期重录；即时写入 `chrome.storage.local`（`dylh_settings`）并 `PreloadManager.applyPerfConfig`；支持一键还原。`modal.CLIP_SETTLE_MS` 改为读设置。

### 测试 / 文档
- 新增 `test/settings-perf.test.mjs`：默认值、钳制、`resetPerfConfig`、生效值。
- 版本升至 1.4.4。

---

## [1.4.3] - 2026-07-25

### 性能
- **出画面即释放加载槽**（`preload.js`）：`playing` 后立刻 `activeLoads--` 并 `_pump()`，加载与录制真正解耦；此前加载槽一直占到录制结束，录制上限 2–3 会堵死整条拉流流水线。`finalizeLoop` / `cleanup` / 早退路径用标志位防二次减槽；录制仍只走 `activeRecords` / `_recordWaiters`。
- **提高视口加载并发**：`MAX_CONCURRENT` 字面/硬上限 **15**，自适应 `max(8, min(15, ceil(cores*1.0)))`（弱机 ≥8、强机到 15）；`MAX_CONCURRENT_RECORD` 弱机 2 / 常见 3 / 强机最高 **4**（`max(2, min(4, ceil(cores*0.35)))`）。
- **录制时长**：`RECORD_MS` 8000→**6000**（加快录制槽周转）；`CLIP_BITRATE` 保持 800k。
- 单元测试：`test/preload.test.mjs`（`npm test`）覆盖并发公式、playing 释槽、`_pump` 续载、录制上限约束。

---

## [1.4.2] - 2026-06-12

### 性能 / 修复
- **多路同时录制卡顿修复**（`preload.js` / `preview.js`）：对比预览打开时此前从不暂停后台预加载墙，叠加抬高的后台基线，多路同时录制时 CPU 饱和。现对比预览与大屏预览打开时，后台墙不仅暂停拉流加载、**也暂停整墙循环播放**（新增 `pausePlayback` / `resumePlayback` + 在途加载完成的自动起播守卫）；录制起录时 `abortInFlight` 立即中止后台在途加载/录制，CPU 全让给前台。
- **录制改硬件 H.264 编码**（`preview.js`）：`_pickRecorderMime` 候选改为 **H.264（`avc1`）优先、回退 VP8**——浏览器 VP8/VP9 的 `MediaRecorder` 是纯 CPU 软编，多路同录易满载；H.264 在多数机器走 GPU 硬件编码，几乎不吃 CPU。码率仍 10Mbps、画质不降。容器/后缀随实际编码走，H.264 录出 `.mp4`/`.mkv`；三处录制入口与 `beforeunload` 全部适配。

### 交互
- **大屏录制提示去重**（`preview.js`）：停止录制不再连弹「录制已停止」+「视频保存成功」两条，只保留 `onstop` 的「视频保存成功」。
- **大屏录制指示器红点闪烁**（`preview.js`）：左下角录制信息由静态「● 录制中」改为红色（`#ff2c55`）闪烁圆点 + 时间，与对比录制指示器同款。

---

## [1.4.1] - 2026-06-07

### 修复 / 调整
- **加载与录制并发分离**（`preload.js`）：恢复加载并发上限 `MAX_CONCURRENT`（自适应、多路同时起播快速出画面），**仅**对「同时录制（编码）」用信号量限制到 `MAX_CONCURRENT_RECORD`（2–3）：起播后申请录制槽，满则该路 live 流继续播、排队等位，结束即释放唤起下一个。削峰只针对编码，不再拖慢出画面。监控面板新增「录制（编码）进行 / 上限」行。
- **录制画质回滚**（`preload.js` / `utils.js`）：`CLIP_BITRATE` 恢复 800000、`RECORD_MS` 恢复 8000、预加载 HLS 配置去掉 `lowLatencyMode:false` 覆盖——瘦身过头导致循环缩略图不够清晰，现复原。
- **导入未能合并修复**（`modal.js`）：备份按钮的提示用了未导入的 `TOAST.TYPE.*` → 抛 `ReferenceError`，被当成「导入失败」且跳过刷新（数据其实已合并落库）。改用字符串字面量 `'success'`/`'error'`，导入合并恢复正常并即时刷新心标。
- **画中画（PiP）入口下线**（`preview.js`）：大屏控制栏不再显示 PiP 按钮；`createPipBtn` 方法**代码保留**，恢复只需取消两处注释。
- **全功能免费开放**：功能门全部放行，顶栏激活入口关闭。

---

## [1.4.0] - 2026-06-07

### 性能
- **限制同时录制（编码削峰）**（`preload.js`）：新增 `MAX_CONCURRENT_RECORD`（默认 3、弱机 2），`_pump` 改用它调度。每路加载即一路 `MediaRecorder` 编码，把同时编码数压到 ≤3，首次填满视口的 CPU 尖峰被压平；未轮到的卡片先显封面/缓存循环。`MAX_CONCURRENT` 作为加载硬上限常量保留。
- **录制参数瘦身**（`preload.js` / `utils.js`）：`CLIP_BITRATE` 800k→400k（编码 CPU≈砍半）、`RECORD_MS` 8s→5s（编码时间/缓存内存 −37%）、预加载 HLS 配置关 `lowLatencyMode`（缩略图不需低延迟）。
- **大列表分批滚动加载**（`modal.js`）：`renderLiveList` 改为先建首批 60 张，末尾哨兵 + `_batchObserver`（提前 600px）滚到底再追加 60 张，避免 500–1000 在播时一次性建几百卡片冻结主线程。搜索/筛选在数据数组上完成、再渲染结果，**不漏匹配**。

### 交互
- **悬浮防误触**（`preview.js`）：把 mouseenter 里**立即**执行的「暂停循环 + 暂停预加载」挪进既有 200ms 去抖回调。鼠标快速横扫一排卡片不再频繁中断循环、不再反复卡住整条预加载；只有真正停留 ≥200ms 才切实时流。
- **大屏画中画（PiP）**（`preview.js`）：大屏控制栏新增 PiP 按钮，把当前主视频送入系统小窗，可一边看播一边翻别的页面；切清晰度后作用于新的主视频；浏览器不支持则隐藏。

### 功能
- **特别关心导入/导出**（`favorite.js` / `modal.js`）：顶栏「备份」按钮弹菜单——导出为 JSON 下载、从 JSON 导入（合并去重）。配合「真卸载会清空本地数据」，提供主动备份/搬家手段。
- **本地观看统计 +「最常看」排序**（新增 `stats.js`；`preview.js`/`card.js`/`modal.js`/`content.js`）：本地记录每个主播本周观看次数（停留 ≥3s 才计、按 ISO 周自动清零，存 `chrome.storage.local`、不上传）；卡片左上显示「本周看 N 次」角标；排序按钮改为三态循环「默认 / 人气 / 最常看」。

### 重构
- `modal.js` 把分散在搜索/特别关心/排序三处的「过滤 + 排序 + 渲染」统一为 `_getViewList()` + `_rerenderView()`，消除重复逻辑；排序状态由 `this.sortMode` 统一管理（替换原 `isSorted` 与读按钮文案的写法）。

---

## [1.3.3] - 2026-06-07

### 性能
- **重渲染流水线硬重置**（`preload.js` 新增 `resetForRerender`，`modal.js` `renderLiveList` 调用）：搜索 / 切特别关心 / 按人气排序时，先中止所有在录任务、暂停并移除旧循环视频（释放解码）、清空排队与可见集，再重建列表。修复两个问题——①「`container.innerHTML=''` 把循环 `<video>` 变游离节点却仍在解码」的**解码泄漏**；②切列表时旧的 20–30 个排队/在录任务占着并发槽，新视口卡片排到队尾、要干等。重置后**当前视口卡片立即优先加载**。**保留 `preloadCache`**，同主播再进视口命中缓存秒显、不重新拉流编码。
- **严格只加载视口内**（`modal.js` `_setupClipObserver`）：加载观察器 `rootMargin` 从 `300px` 收紧到 `0`——视口外卡片不再提前拉流编码，离开视口即中止释放。播放门控观察器（仅完整可见才播）保持不变。
- **离开视口顺带踢出排队**（`preload.js` `release`）：卡片移出视口时除中止在录、移除循环视频外，新增从 `queue` 删除该 room 并清 `queued` 状态，使排队列表持续等于当前视口。

### 说明
- 本次**不调并发上限** `MAX_CONCURRENT`、**不删除已完成片段缓存**。卡顿缓解来自「消除游离解码 + 离屏不再编码 + 视口优先」。

---

## [1.3.2] - 2026-06-06

### 新增
- **视口播放门控**（`modal.js` 新增 `_setupPlaybackObserver`）：直播墙里仅**完整可见**的卡片（intersectionRatio ≥ 0.999）才播放循环视频；任何被滚动裁切的卡片立即暂停。这意味着同屏播放路数被严格限制为当前视口能完全容纳的卡片数（典型 12 路上下），其余卡片只占内存不占解码。与既有 `_clipObserver`（rootMargin: 300px 提前加载）解耦——加载/缓存策略不变，仅"是否播放"被收紧。
- 配套：preload.js 的 `attachLoop` / `finalizeLoop` / `resumeCard` 在播放前检查 `cardPreview._shouldPlay`，处理「加载完成时卡片正好不完全可见」的竞态。

---

## [1.3.1] - 2026-06-06

### 变更
- **清晰度切换改为「续播 1.5s 再硬切」**（`preview.js`）：弃用原有透明度/音量交叉淡入。新流出画面（`canplay`）后**继续播放 1.5 秒再一次性切换直播源**，简单稳定，避免淡入期间黑屏/闪缩/同步出错；录制中、三联屏仍禁用切换；`canplay` 未触发 8s 兜底直接切；新流致命错误前回滚。
- **「对比预览」按钮改名为「N 同时看」**（`modal.js`）：去掉两矩形 SVG 图标，计数前置（如「2 同时看」「3 同时看」），放宽内边距 + `white-space: nowrap`，避免按钮在窄屏下被挤断行。
- **清晰度档位菜单宽度对齐按钮**（`preview.js`）：菜单宽度自动等于按钮；档位项 padding 收窄，短文案仍居中。
- **ESC 关闭顺序前移**（`modal.js`）：浮层面板叠在大屏之上时，ESC 优先关掉最上层浮层；二者都不在时再交给大屏自身 ESC 或关闭列表。

### 文档修订
- 特别关心 / 设置实际使用 `chrome.storage.local`（按扩展隔离、跨 www/live 子域共享、跨设备**不**同步；真卸载会清空）。统一改正此前误写为 `sync` 的描述。

---

## [1.1.0] - 2026-06-02

### 新增
- **特别关心**：卡片标记主播为特别关心，顶部可一键筛选；改用 `chrome.storage.local` 存储（`favorite.js`），跨 www/live 子域共享，并自动迁移旧 localStorage 数据。
- **全局设置**：新增 `settings.js`，音量与声音总开关用 `chrome.storage.local` 持久化，悬浮预览与大屏预览声音状态互相同步。
- **声音总开关**：列表顶部"放音/静音"按钮，控制预览是否出声；全局音量记忆（如调到 50%，下次预览仍为 50%）。
- **大屏预览**（原"全屏预览"更名）：默认铺满整屏(100%)，新增浏览器全屏(F11)按钮、ESC 分级退出、右下角控件自动隐显。
- **三联屏**：同一路画面用 `captureStream` 镜像并排，零缝隙拼接、共享解码不卡顿；默认单屏，按钮切换。
- **多路对比预览**（`openComparePreview`）：卡片右上角复选框勾选最多 3 个不同直播间同屏对比；网格布局复用三联屏样式，竖屏无黑边、零缝隙拼接；每路独立控声/调音量；新增浏览器全屏按钮。
- **直播录制**：
  - 大屏单路录制（含 `beforeunload` 保护，关闭/刷新自动保存已录内容）。
  - 多路对比：每路单独录制 + 合并录制（canvas 拼接画面 + Web Audio 混音 → 单文件），二者互斥。
  - 统一高码率保证清晰度；文件名区分 `抖音对比-单路_…` / `抖音对比-合并_…`。
- **截图作封面**：悬浮预览结束后捕获最后一帧作为卡片封面并缓存，带"已预览"标记，跨搜索/排序/重开列表保留。
- **氛围词条**：支持自定义、为每个主播单独配置、开关显示，含 TTS 语音播报。
- **资源监控**：顶部按钮查看内存 / HLS 实例 / 视频元素占用（`monitor.js`）。
- **交互**：头像/标题悬浮提示；列表与各预览面板均支持 ESC 关闭。
- **加载三态**：列表加载显示 loading 动画；无数据显示"当前没有正在关注的直播"；失败显示加载失败提示。
- **清除已选**：列表顶部新增"清除已选"按钮，一键取消所有对比勾选；刷新列表时也会自动清除选中。

### 优化
- 菜单注入与持久化适配抖音 React 重渲染。
- 音量控件改为按钮 + 滑块组合，默认音量 30%。
- 录制编码兼顾清晰度与可靠保存。
- 取消列表默认批量预加载，改为悬浮某卡片时按需加载，减少频繁请求导致的卡顿。

### 修复
- 修复悬浮预览播放失败后对比复选框失效。
- 修复对比复选框偶现"取消不掉"（身份键由可能为空的 `secUid` 改为唯一的 `roomUrl`）。
- 修复合并录制无法保存。
- 修复录制计时不显示。
- 修复 `captureLastFramePreview` 在视频未就绪时报 `IndexSizeError`、`playVideo` 的 `parentElement` 空引用、`play()` 被 `pause()` 打断的 `AbortError`。
- 修复浏览器全屏后控制菜单无法再次显示。
- 修复菜单图标右边距、三联屏图标过小等 UI 细节。
- 对比预览 ESC 行为对齐大屏预览：全屏时 ESC 退全屏、非全屏时关闭面板。
- 大屏/对比预览控件显隐改用 `visibility` + `opacity` 双控。

---

## [1.0.0]

### 新增
- 在抖音网页版导航栏添加"关注直播"菜单，方块卡片式展示关注主播的直播列表。
- 直播卡片：封面、标题、头像、昵称、在线人数。
- 列表支持搜索（昵称/标题）、按人气排序、回到顶部、刷新。
- 悬浮预览直播画面，支持全屏预览。
- 点击画面进入直播间，点击头像进入主播主页。
- 智能检测连麦画面并自动调整显示比例。
- 自动适配抖音深色/浅色模式。
- 通过 MAIN 世界桥接脚本（`bridge.js`）复用抖音反爬签名请求关注直播数据。
