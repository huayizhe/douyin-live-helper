# 更新日志

本文件记录「抖音关注直播助手」的主要功能迭代与修复。
版本号遵循 `主版本.次版本.修订` 约定；日期格式 `YYYY-MM-DD`。

---

## [1.4.3] - 2026-07-25

### 性能
- **出画面即释放加载槽**（`preload.js`）：`playing` 后立刻 `activeLoads--` 并 `_pump()`，加载与录制真正解耦；此前加载槽一直占到录制结束，录制上限 2–3 会堵死整条拉流流水线。`finalizeLoop` / `cleanup` / 早退路径用标志位防二次减槽；录制仍只走 `activeRecords` / `_recordWaiters`。
- **提高视口加载并发**：`MAX_CONCURRENT` 字面/硬上限 **15**，自适应 `max(8, min(15, ceil(cores*1.0)))`（弱机 ≥8、强机到 15）；`MAX_CONCURRENT_RECORD` 弱机 2 / 常见 3 / 强机最高 **4**（`max(2, min(4, ceil(cores*0.35)))`）。
- **录制时长**：`RECORD_MS` 8000→**6000**（加快录制槽周转）；`CLIP_BITRATE` 保持 800k。
- 单元测试：`test/preload.test.mjs`（`npm test`）覆盖并发公式、playing 释槽、`_pump` 续载、录制上限约束。

---

## [1.4.2] - 2026-06-12

### 性能 / 修复
- **多路同时录制卡顿修复**（`preload.js` / `preview.js`）：「同时看」对比预览打开时此前**从不暂停**后台预加载墙（自 v1.1.0 的缺口），叠加 1.4.1 抬高的后台基线，多路同时录制时 CPU 饱和（实测 2 路 60%+、3 路瞬间卡顿）。现对比预览与大屏预览打开时，后台墙不仅暂停拉流加载、**也暂停整墙循环播放**（新增 `pausePlayback` / `resumePlayback` + 在途加载完成的自动起播守卫）；录制起录时 `abortInFlight` 立即中止后台在途加载/录制，CPU 全让给前台。
- **录制改硬件 H.264 编码**（`preview.js`）：`_pickRecorderMime` 候选改为 **H.264（`avc1`）优先、回退 VP8**——浏览器 VP8/VP9 的 `MediaRecorder` 是纯 CPU 软编，多路同录易满载；H.264 在多数机器走 GPU 硬件编码，几乎不吃 CPU。码率仍 10Mbps、画质不降。容器/后缀随实际编码走（新增 `_recContainer`，`_downloadWebm`→`_downloadRec`），H.264 录出 `.mp4`/`.mkv` 不再误存为 `.webm`；三处录制入口与 `beforeunload` 全部适配。

### 交互
- **大屏录制提示去重**（`preview.js`）：停止录制不再连弹「录制已停止」+「视频保存成功」两条，只保留 `onstop` 的「视频保存成功」。
- **大屏录制指示器红点闪烁**（`preview.js`）：左下角录制信息由静态「● 录制中」改为红色（`#ff2c55`）闪烁圆点 + 时间，与对比录制指示器同款。

---

## [1.4.1] - 2026-06-07

### 修复 / 调整
- **加载与录制并发分离**（`preload.js`）：1.4.0 把「同时加载」也压到 3 导致铺画面变慢。现恢复加载并发上限 `MAX_CONCURRENT`（自适应、多路同时起播快速出画面），**仅**对「同时录制（编码）」用信号量限制到 `MAX_CONCURRENT_RECORD`（2–3）：起播后申请录制槽，满则该路 live 流继续播、排队等位，结束即释放唤起下一个。削峰只针对编码，不再拖慢出画面。监控面板新增「录制（编码）进行 / 上限」行。
- **录制画质回滚**（`preload.js` / `utils.js`）：`CLIP_BITRATE` 恢复 800000、`RECORD_MS` 恢复 8000、预加载 HLS 配置去掉 `lowLatencyMode:false` 覆盖——1.4.0 瘦身过头导致循环缩略图不够清晰，现复原。
- **导入未能合并修复**（`modal.js`）：备份按钮的提示用了未导入的 `TOAST.TYPE.*` → 抛 `ReferenceError`，被当成「导入失败」且跳过刷新（数据其实已合并落库）。改用字符串字面量 `'success'`/`'error'`，导入合并恢复正常并即时刷新心标。
- **画中画（PiP）入口下线**（`preview.js`）：大屏控制栏不再显示 PiP 按钮；`createPipBtn` 方法**代码保留**，恢复只需取消两处注释。
- **会员入口下线**（`constants.js` / `license.js` / `modal.js`）：新增总开关 `LICENSE.ENABLED=false`——全功能解锁（4 个功能门经 `isPro` 自动放行）、隐藏顶栏「激活 / ✓ PRO」按钮。许可证全部代码（验签、激活、换绑、弹窗、签发工具）**原样保留**，置回 `true` 即恢复收费体系。

---

## [1.4.0] - 2026-06-07

### 性能
- **限制同时录制（编码削峰）**（`preload.js`）：新增 `MAX_CONCURRENT_RECORD`（默认 3、弱机 2），`_pump` 改用它调度。每路加载即一路 `MediaRecorder` 编码，把同时编码数压到 ≤3，首次填满视口的 CPU 尖峰被压平；未轮到的卡片先显封面/缓存循环。`MAX_CONCURRENT` 作为加载硬上限常量保留。
- **录制参数瘦身**（`preload.js` / `utils.js`）：`CLIP_BITRATE` 800k→400k（编码 CPU≈砍半）、`RECORD_MS` 8s→5s（编码时间/缓存内存 −37%）、预加载 HLS 配置关 `lowLatencyMode`（缩略图不需低延迟）。
- **大列表分批滚动加载**（`modal.js`）：`renderLiveList` 改为先建首批 60 张，末尾哨兵 + `_batchObserver`（提前 600px）滚到底再追加 60 张，避免 500–1000 在播时一次性建几百卡片冻结主线程。搜索/筛选在数据数组上完成、再渲染结果，**不漏匹配**。

### 交互
- **悬浮防误触**（`preview.js`）：把 mouseenter 里**立即**执行的「暂停循环 + 暂停预加载」挪进既有 200ms 去抖回调。鼠标快速横扫一排卡片不再频繁中断循环、不再反复卡住整条预加载；只有真正停留 ≥200ms 才切实时流。
- **大屏画中画（PiP）**（`preview.js`）：大屏控制栏新增 PiP 按钮，把当前主视频送入系统小窗，可一边看播一边翻别的页面；切清晰度后作用于新的主视频；浏览器不支持则隐藏。

### 功能
- **特别关心导入/导出**（`favorite.js` / `modal.js`）：顶栏「备份」按钮弹菜单——导出为 JSON 下载、从 JSON 导入（合并去重）。配合「真卸载会清空本地数据」，提供主动备份/搬家手段。
- **本地观看统计 +「最常看」排序**（新增 `stats.js`；`preview.js`/`card.js`/`modal.js`/`content.js`）：本地记录每个主播本周观看次数（停留 ≥3s 才计、按 ISO 周自动清零，存 `chrome.storage.local`、不上传）；卡片左上显示「本周看 N 次」角标；排序按钮改为三态循环「默认 / 人气 / 最常看」。

### 重构
- `modal.js` 把分散在搜索/特别关心/排序三处的「过滤 + 排序 + 渲染」统一为 `_getViewList()` + `_rerenderView()`，消除重复逻辑；排序状态由 `this.sortMode` 统一管理（替换原 `isSorted` 与读按钮文案的写法）。

---

## [1.3.3] - 2026-06-07

### 性能
- **重渲染流水线硬重置**（`preload.js` 新增 `resetForRerender`，`modal.js` `renderLiveList` 调用）：搜索 / 切特别关心 / 按人气排序时，先中止所有在录任务、暂停并移除旧循环视频（释放解码）、清空排队与可见集，再重建列表。修复两个问题——①「`container.innerHTML=''` 把循环 `<video>` 变游离节点却仍在解码」的**解码泄漏**（监控里曾出现「我方循环 32 > DOM 视频 28」、解码路数随切换次数累积上涨）；②切列表时旧的 20–30 个排队/在录任务占着并发槽，新视口卡片排到队尾、要干等。重置后**当前视口卡片立即优先加载**。**保留 `preloadCache`**，同主播再进视口命中缓存秒显、不重新拉流编码。
- **严格只加载视口内**（`modal.js` `_setupClipObserver`）：加载观察器 `rootMargin` 从 `300px` 收紧到 `0`——视口外卡片不再提前拉流编码，离开视口即中止释放。播放门控观察器（1.3.2，仅完整可见才播）保持不变。
- **离开视口顺带踢出排队**（`preload.js` `release`）：卡片移出视口时除中止在录、移除循环视频外，新增从 `queue` 删除该 room 并清 `queued` 状态，使排队列表持续等于当前视口（不止重渲染那一刻）。

### 说明
- 本次**不调并发上限** `MAX_CONCURRENT`（仍 `cores*0.75`，≤9）、**不删除已完成片段缓存**（blob 靠现有 LRU 管理，离屏只释放解码、保留缓存以便滚回秒显）。卡顿缓解来自「消除游离解码 + 离屏不再编码 + 视口优先」。若后续仍偏卡，再考虑单独限制同时录制路数。

---

## [1.3.2] - 2026-06-06

### 新增
- **视口播放门控**（`modal.js` 新增 `_setupPlaybackObserver`）：直播墙里仅**完整可见**的卡片（intersectionRatio ≥ 0.999）才播放循环视频；任何被滚动裁切的卡片立即暂停。这意味着同屏播放路数被严格限制为当前视口能完全容纳的卡片数（典型 12 路上下），其余卡片只占内存不占解码。与既有 `_clipObserver`（rootMargin: 300px 提前加载）解耦——加载/缓存策略不变（提前缓冲、滚回不重下），仅"是否播放"被收紧。
- 配套：preload.js 的 `attachLoop` / `finalizeLoop` / `resumeCard` 在播放前检查 `cardPreview._shouldPlay`，处理「加载完成时卡片正好不完全可见」的竞态（IntersectionObserver 不会自动再触发）。

### 变更
- **会员信息弹窗去掉「关闭」按钮**（`license.js` `_statusHTML`）：右上角已有 `×` 关闭，按钮行重复。去掉后按钮行变为「立即续费 / 解除本设备激活」两项（或终身套餐时仅「解除本设备激活」一项），不再拥挤。
- **「解除本设备激活」按钮不再换行**（`css/style.css` `.dylh-dialog-btn-danger`）：增加 `white-space: nowrap`、`padding` 从 `10px 0` 改为 `10px 12px`，配合上一条「去关闭按钮」后剩余宽度足够，文案永远单行。

---

## [1.3.1] - 2026-06-06

### 变更
- **清晰度切换改为「续播 1.5s 再硬切」**（`preview.js`：`_crossfadeBigScreenQuality` → `_switchBigScreenQuality`）：弃用原有透明度/音量交叉淡入。新流出画面（`canplay`）后**继续播放 1.5 秒再一次性切换直播源**，简单稳定，避免淡入期间黑屏/闪缩/同步出错；录制中、三联屏仍禁用切换；`canplay` 未触发 8s 兜底直接切；新流致命错误前回滚。
- **「对比预览」按钮改名为「N 同时看」**（`modal.js` `createCompareButton` / `updateCompareButton`）：去掉两矩形 SVG 图标，计数前置（如「2 同时看」「3 同时看」），放宽内边距 + `white-space: nowrap`，避免按钮在窄屏下被挤断行。
- **清晰度档位菜单宽度对齐按钮**（`preview.js` `createQualityBtn`）：菜单从 `right:0; minWidth:72px` 改为 `left:0; right:0`，宽度自动等于按钮；档位项 padding 收窄到 `8px 6px`，短文案仍居中。
- **设备识别码改为浏览器指纹确定性派生**（`license.js`：新增 `_computeFingerprint`）：原方案是首次生成随机 UUID 后存 `storage.local`，**插件被移除后再添加**会清空 local → 设备码变化 → 旧激活码 `payload.m` 不匹配而失效。新方案在缓存缺失时由稳定信号（`platform`、`hardwareConcurrency`、`deviceMemory`、屏幕尺寸/色深、语言、时区、`maxTouchPoints`）做 `SHA-256`，取前 32 位 hex 加 `fp-` 前缀。重装后能再次算出**同一个**设备码，旧激活码重新粘贴即可恢复 PRO；指纹失败兜底为随机值。激活码绑定/验签逻辑（`_verify`/`activate`/`deactivate`）保持不变。
- **ESC 关闭顺序前移**（`modal.js` `disableDouyinShortcuts`）：原顺序在大屏/对比/全屏时直接 `return`，导致**叠在大屏之上**的 PRO 激活弹窗、会员信息弹窗、资源监控面板按 ESC 无反应。现把这两类浮层的关闭判定提到大屏早退之前，ESC 在所有场景下都能优先关掉最上层浮层；二者都不在时再交给大屏自身 ESC 或关闭列表。

### 修复
- **重装插件后 PRO 激活码失效**：见上条「设备识别码确定性派生」。前提：用户保留 `chrome.storage.local`（真卸载必然清空本地数据，未迁 `storage.sync`），仅消除「设备码漂移」这一项失效原因，旧激活码仍可用。
- **PRO 激活弹窗/会员信息弹窗/资源面板叠在大屏之上时按 ESC 关不掉**：见上条「ESC 关闭顺序前移」。

### 迁移提示
- 设备码生成口径变了。**已用旧随机 UUID 设备码签发过的激活码**，重装后设备码会从 UUID 变成指纹码，旧码 `payload.m` 不再匹配——需用 `tools/issue-license.js --machine=<新设备码>` 重新签发一次。项目早期、激活码自签可控，影响有限。

### 文档修订
- `DEV.md` / `README.md` 中「特别关心 / 设置使用 `chrome.storage.sync`」的描述与代码不符。实际从 1.1.0 起这两项就用 `chrome.storage.local`（按扩展隔离、跨 www/live 子域共享、跨设备**不**同步；真卸载会清空）。本次统一改正。

---

## [1.3.0] - 2026-06-04

### 新增
- **在线订阅支付**：升级弹窗内选套餐 → 弹支付二维码 → 微信/支付宝扫码付款 → 插件自动轮询、自动激活，全程无需人工发卡。复用既有 ECDSA 验签内核（服务器用同一私钥签发，格式不变）。
- **许可证服务器**（新增 `server/`）：Node + Express。下单 `/api/order/create`、轮询 `/api/order/status`、支付回调 `/api/pay/notify/{alipay|wechat}`、换绑 `/api/rebind`。支付层**适配器模式**（`pay/alipay.js`、`pay/wechat.js`、`pay/mock.js`），加/换渠道不动业务代码。
- **套餐可点击单选**：月/年/买断三卡点击切换高亮（默认年付），选中后下单。
- **设备换绑**：换新设备时在「激活 PRO」粘贴原授权，自动调用换绑接口、绑定新设备并保留剩余时长。
- **到期提醒与续费**：临近到期（≤7 天）徽章变橙色提示「剩 N 天」，点击一键续费；续费到期自动接续在原到期之后。
- **手动激活兜底**：保留 `issue-license.js` 离线签发与弹窗内「手动激活」入口，服务器故障时仍可发卡。

### 变更
- `constants.js` 的 `LICENSE` 新增 `SERVER`，`PRICING` 每档补 `amount`(分)/`days`(天)。
- `manifest.json` `host_permissions` 增加许可证服务器域名（开发期含 `http://localhost:3000`）。
- 文档：服务器部署手册重写为官方支付版；收款手册改为运营手册；插件测试手册补在线联调。

### 说明
- 个人/个体户无法做"自动扣费续订"，"订阅"= 在线开通 + 到期手动续费。
- 在线支付需个体户执照 + 微信/支付宝商户号 + 服务器 + 域名 + HTTPS（备案）；商户号办理期间可用 mock/沙箱先行联调。

---

## [1.2.0] - 2026-06-03

### 新增
- **PRO 付费订阅体系**：引入免费 / PRO 分层。免费版保留全部基础看播体验，PRO 解锁高阶功能。套餐**只按时长分级**（月付 / 年付 / 永久买断），不按功能阉割——任一套餐都解锁全部 PRO 功能。
- **离线许可证授权**（`license.js`）：基于 ECDSA P-256（ES256）非对称签名。服务方用私钥签发许可证字符串，插件内置公钥**本地验签**，全程无需联网；许可证含到期时间与可选机器绑定（防转卖），篡改任意字节即验签失败。
- **PRO 功能锁（展示并锁定）**：以下功能对免费用户正常展示，点击弹出权益对比/升级页——
  - 多路对比预览（2–3 个不同主播同屏）
  - 直播录制（大屏单路 / 对比各路独立 / 多路合并 + 混音）
  - TTS 氛围词条 + 语音播报
- **权益对比升级页**：免费 vs PRO 逐条对比（PRO 权益拆细展示）+ 三档价格卡 + 密钥激活框；标题栏新增 PRO 状态徽章，可查看授权状态、解除本机激活、查看本机标识。
- **命令行签发工具**（`tools/`）：`keygen-pair.js` 生成密钥对并自动写入公钥；`issue-license.js` 收款后离线签发许可证（支持 `--plan` / `--days` / `--machine`）。
- **版本管理**：项目纳入 Git；新增 `.gitignore`（排除 `node_modules`、私钥 `tools/private-key.json`、`.env` 等敏感与可再生文件）。
- **运营文档**（`docs/`）：新增《收款发卡手册》《服务器部署手册》《插件配置与测试手册》。

### 说明
- 三联屏镜像、特别关心数量、资源监控保持**免费**（不设付费墙）。
- 私钥 `tools/private-key.json` 绝不可外泄或提交版本库；丢失或泄露需重新签发全部许可证。
- 阶段二（爱发电 Webhook 24 小时自动发卡）为后续迭代，当前为半自动收款 + 手动签发。

---

## [1.1.0] - 2026-06-02

### 新增
- **特别关心**：卡片标记主播为特别关心，顶部可一键筛选；改用 `chrome.storage.sync` 存储（`favorite.js`），跨 www/live 子域、跨设备同步，并自动迁移旧 localStorage 数据。
- **全局设置**：新增 `settings.js`，音量与声音总开关用 `chrome.storage.sync` 持久化，悬浮预览与大屏预览声音状态互相同步。
- **声音总开关**：列表顶部"放音/静音"按钮，控制预览是否出声；全局音量记忆（如调到 50%，下次预览仍为 50%）。
- **大屏预览**（原"全屏预览"更名）：默认铺满整屏(100%)，新增浏览器全屏(F11)按钮、ESC 分级退出、右下角控件自动隐显。
- **三联屏**：同一路画面用 `captureStream` 镜像并排，零缝隙拼接、共享解码不卡顿；默认单屏，按钮切换。
- **多路对比预览**（`openComparePreview`）：卡片右上角复选框勾选最多 3 个不同直播间同屏对比；网格布局复用三联屏样式，竖屏无黑边、零缝隙拼接；每路独立控声/调音量；新增浏览器全屏按钮。
- **直播录制**：
  - 大屏单路录制（含 `beforeunload` 保护，关闭/刷新自动保存已录内容）。
  - 多路对比：每路单独录制 + 合并录制（canvas 拼接画面 + Web Audio 混音 → 单文件），二者互斥。
  - 统一高码率（vp8 + 10Mbps）保证清晰度；文件名区分 `抖音对比-单路_…` / `抖音对比-合并_…`。
- **截图作封面**：悬浮预览结束后捕获最后一帧作为卡片封面并缓存，带"已预览"标记，跨搜索/排序/重开列表保留。
- **氛围词条**：支持自定义、为每个主播单独配置、开关显示，含 TTS 语音播报。
- **资源监控**：顶部按钮查看内存 / HLS 实例 / 视频元素占用（`monitor.js`）。
- **交互**：头像/标题悬浮提示；列表与各预览面板均支持 ESC 关闭。
- **加载三态**：列表加载显示 loading 动画；无数据显示"当前没有正在关注的直播"；失败显示加载失败提示。
- **清除已选**：列表顶部新增"清除已选"按钮，一键取消所有对比勾选；刷新列表时也会自动清除选中。

### 优化
- 菜单注入与持久化适配抖音 React 重渲染。
- 音量控件改为按钮 + 滑块组合，默认音量 30%。
- 录制编码统一为 vp8 高码率，兼顾清晰度与可靠保存。
- 取消列表默认批量预加载，改为悬浮某卡片时按需加载，减少频繁请求导致的卡顿。

### 修复
- 修复悬浮预览播放失败后对比复选框失效（`showPlaybackError` 用 `innerHTML +=` 重建 DOM、丢失监听 → 改为非破坏性 `appendChild`）。
- 修复对比复选框偶现"取消不掉"（身份键由可能为空的 `secUid` 改为唯一的 `roomUrl`）。
- 修复合并录制无法保存（录制编码从 vp9 回退到对 canvas 流可靠的 vp8）。
- 修复录制计时不显示（`requestAnimationFrame` 丢失 `this` 与参数）。
- 修复 `captureLastFramePreview` 在视频未就绪时报 `IndexSizeError`、`playVideo` 的 `parentElement` 空引用、`play()` 被 `pause()` 打断的 `AbortError`。
- 修复浏览器全屏后控制菜单无法再次显示（改 `mousemove` 显示 + 空闲自动隐藏）。
- 修复菜单图标右边距、三联屏图标过小等 UI 细节。
- 对比预览 ESC 行为对齐大屏预览：全屏时 ESC 退全屏、非全屏时关闭面板。
- 大屏/对比预览控件显隐改用 `visibility` + `opacity` 双控，修复 Edge 全屏下控件不重绘、"看不见却能点"的问题（待 Edge 实测确认）。

---

## [1.0.0]

### 新增
- 在抖音网页版导航栏添加"关注直播"菜单，方块卡片式展示关注主播的直播列表。
- 直播卡片：封面、标题、头像、昵称、在线人数。
- 列表支持搜索（昵称/标题）、按人气排序、回到顶部、刷新。
- 悬浮预览直播画面，支持全屏预览。
- 点击画面进入直播间，点击头像进入主播主页。
- 智能检测连麦画面并自动调整显示比例。
- 自动适配抖音深色/浅色模式。
- 通过 MAIN 世界桥接脚本（`bridge.js`）复用抖音反爬签名请求关注直播数据。
