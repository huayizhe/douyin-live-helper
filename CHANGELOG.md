# 更新日志

本文件记录「抖音关注直播助手」的主要功能迭代与修复。
版本号遵循 `主版本.次版本.修订` 约定；日期格式 `YYYY-MM-DD`。

---

## [1.3.0] - 2026-06-04

### 新增
- **在线订阅支付**：升级弹窗内选套餐 → 弹支付二维码 → 微信/支付宝扫码付款 → 插件自动轮询、自动激活，全程无需人工发卡。复用既有 ECDSA 验签内核（服务器用同一私钥签发，格式不变）。
- **许可证服务器**（新增 `server/`）：Node + Express。下单 `/api/order/create`、轮询 `/api/order/status`、支付回调 `/api/pay/notify/{alipay|wechat}`、换绑 `/api/rebind`。支付层**适配器模式**（`pay/alipay.js`、`pay/wechat.js`、`pay/mock.js`），加/换渠道不动业务代码。
- **套餐可点击单选**：月/年/买断三卡点击切换高亮（默认年付），选中后下单。
- **设备换绑**：换新设备时在「激活 PRO」粘贴原授权，自动调用换绑接口、绑定新设备并保留剩余时长。
- **到期提醒与续费**：临近到期（≤7 天）徽章变橙色提示「剩 N 天」，点击一键续费；续费到期自动接续在原到期之后。
- **手动激活兜底**：保留 `issue-license.js` 离线签发与弹窗内「手动激活」入口，服务器故障时仍可发卡。

### 变更
- `constants.js` 的 `LICENSE` 新增 `SERVER`，`PRICING` 每档补 `amount`(分)/`days`(天)。
- `manifest.json` `host_permissions` 增加许可证服务器域名（开发期含 `http://localhost:3000`）。
- 文档：服务器部署手册重写为官方支付版；收款手册改为运营手册；插件测试手册补在线联调。

### 说明
- 个人/个体户无法做"自动扣费续订"，"订阅"= 在线开通 + 到期手动续费。
- 在线支付需个体户执照 + 微信/支付宝商户号 + 服务器 + 域名 + HTTPS（备案）；商户号办理期间可用 mock/沙箱先行联调。

---

## [1.2.0] - 2026-06-03

### 新增
- **PRO 付费订阅体系**：引入免费 / PRO 分层。免费版保留全部基础看播体验，PRO 解锁高阶功能。套餐**只按时长分级**（月付 / 年付 / 永久买断），不按功能阉割——任一套餐都解锁全部 PRO 功能。
- **离线许可证授权**（`license.js`）：基于 ECDSA P-256（ES256）非对称签名。服务方用私钥签发许可证字符串，插件内置公钥**本地验签**，全程无需联网；许可证含到期时间与可选机器绑定（防转卖），篡改任意字节即验签失败。
- **PRO 功能锁（展示并锁定）**：以下功能对免费用户正常展示，点击弹出权益对比/升级页——
  - 多路对比预览（2–3 个不同主播同屏）
  - 直播录制（大屏单路 / 对比各路独立 / 多路合并 + 混音）
  - TTS 氛围词条 + 语音播报
- **权益对比升级页**：免费 vs PRO 逐条对比（PRO 权益拆细展示）+ 三档价格卡 + 密钥激活框；标题栏新增 PRO 状态徽章，可查看授权状态、解除本机激活、查看本机标识。
- **命令行签发工具**（`tools/`）：`keygen-pair.js` 生成密钥对并自动写入公钥；`issue-license.js` 收款后离线签发许可证（支持 `--plan` / `--days` / `--machine`）。
- **版本管理**：项目纳入 Git；新增 `.gitignore`（排除 `node_modules`、私钥 `tools/private-key.json`、`.env` 等敏感与可再生文件）。
- **运营文档**（`docs/`）：新增《收款发卡手册》《服务器部署手册》《插件配置与测试手册》。

### 说明
- 三联屏镜像、特别关心数量、资源监控保持**免费**（不设付费墙）。
- 私钥 `tools/private-key.json` 绝不可外泄或提交版本库；丢失或泄露需重新签发全部许可证。
- 阶段二（爱发电 Webhook 24 小时自动发卡）为后续迭代，当前为半自动收款 + 手动签发。

---

## [1.1.0] - 2026-06-02

### 新增
- **特别关心**：卡片标记主播为特别关心，顶部可一键筛选；改用 `chrome.storage.sync` 存储（`favorite.js`），跨 www/live 子域、跨设备同步，并自动迁移旧 localStorage 数据。
- **全局设置**：新增 `settings.js`，音量与声音总开关用 `chrome.storage.sync` 持久化，悬浮预览与大屏预览声音状态互相同步。
- **声音总开关**：列表顶部"放音/静音"按钮，控制预览是否出声；全局音量记忆（如调到 50%，下次预览仍为 50%）。
- **大屏预览**（原"全屏预览"更名）：默认铺满整屏(100%)，新增浏览器全屏(F11)按钮、ESC 分级退出、右下角控件自动隐显。
- **三联屏**：同一路画面用 `captureStream` 镜像并排，零缝隙拼接、共享解码不卡顿；默认单屏，按钮切换。
- **多路对比预览**（`openComparePreview`）：卡片右上角复选框勾选最多 3 个不同直播间同屏对比；网格布局复用三联屏样式，竖屏无黑边、零缝隙拼接；每路独立控声/调音量；新增浏览器全屏按钮。
- **直播录制**：
  - 大屏单路录制（含 `beforeunload` 保护，关闭/刷新自动保存已录内容）。
  - 多路对比：每路单独录制 + 合并录制（canvas 拼接画面 + Web Audio 混音 → 单文件），二者互斥。
  - 统一高码率（vp8 + 10Mbps）保证清晰度；文件名区分 `抖音对比-单路_…` / `抖音对比-合并_…`。
- **截图作封面**：悬浮预览结束后捕获最后一帧作为卡片封面并缓存，带"已预览"标记，跨搜索/排序/重开列表保留。
- **氛围词条**：支持自定义、为每个主播单独配置、开关显示，含 TTS 语音播报。
- **资源监控**：顶部按钮查看内存 / HLS 实例 / 视频元素占用（`monitor.js`）。
- **交互**：头像/标题悬浮提示；列表与各预览面板均支持 ESC 关闭。
- **加载三态**：列表加载显示 loading 动画；无数据显示"当前没有正在关注的直播"；失败显示加载失败提示。
- **清除已选**：列表顶部新增"清除已选"按钮，一键取消所有对比勾选；刷新列表时也会自动清除选中。

### 优化
- 菜单注入与持久化适配抖音 React 重渲染。
- 音量控件改为按钮 + 滑块组合，默认音量 30%。
- 录制编码统一为 vp8 高码率，兼顾清晰度与可靠保存。
- 取消列表默认批量预加载，改为悬浮某卡片时按需加载，减少频繁请求导致的卡顿。

### 修复
- 修复悬浮预览播放失败后对比复选框失效（`showPlaybackError` 用 `innerHTML +=` 重建 DOM、丢失监听 → 改为非破坏性 `appendChild`）。
- 修复对比复选框偶现"取消不掉"（身份键由可能为空的 `secUid` 改为唯一的 `roomUrl`）。
- 修复合并录制无法保存（录制编码从 vp9 回退到对 canvas 流可靠的 vp8）。
- 修复录制计时不显示（`requestAnimationFrame` 丢失 `this` 与参数）。
- 修复 `captureLastFramePreview` 在视频未就绪时报 `IndexSizeError`、`playVideo` 的 `parentElement` 空引用、`play()` 被 `pause()` 打断的 `AbortError`。
- 修复浏览器全屏后控制菜单无法再次显示（改 `mousemove` 显示 + 空闲自动隐藏）。
- 修复菜单图标右边距、三联屏图标过小等 UI 细节。
- 对比预览 ESC 行为对齐大屏预览：全屏时 ESC 退全屏、非全屏时关闭面板。
- 大屏/对比预览控件显隐改用 `visibility` + `opacity` 双控，修复 Edge 全屏下控件不重绘、"看不见却能点"的问题（待 Edge 实测确认）。

---

## [1.0.0]

### 新增
- 在抖音网页版导航栏添加"关注直播"菜单，方块卡片式展示关注主播的直播列表。
- 直播卡片：封面、标题、头像、昵称、在线人数。
- 列表支持搜索（昵称/标题）、按人气排序、回到顶部、刷新。
- 悬浮预览直播画面，支持全屏预览。
- 点击画面进入直播间，点击头像进入主播主页。
- 智能检测连麦画面并自动调整显示比例。
- 自动适配抖音深色/浅色模式。
- 通过 MAIN 世界桥接脚本（`bridge.js`）复用抖音反爬签名请求关注直播数据。
